services:
  web:
    image: python:3.10-slim
    container_name: compose_webapp
    working_dir: /app
    volumes:
      - .:/app
    command: bash -c "pip install flask && python app.py"
    ports:
      - "5001:5000"